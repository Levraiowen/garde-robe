# Copie ce fichier en `.env` pour forcer l'adresse de l'API.
# Sans ce fichier, l'app utilise automatiquement l'IP de ton PC (celle du serveur Expo) sur le port 8000.
# EXPO_PUBLIC_API_URL=http://192.168.1.12:8000
